# Experiment with Google Test

[based on this page of the docs](https://google.github.io/googletest/quickstart-cmake.html)

````bash
cmake -S . -B build
cmake --build build

# run tests:
cd build && ctest
````

